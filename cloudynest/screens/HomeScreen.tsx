import { View, Stack, Text, ScrollView } from "native-base";
import React from "react";

export default function HomeScreen() {
	return (
		<ScrollView w={"full"}>
			<Stack flex={1} bg="red.100"></Stack>
		</ScrollView>
	);
}
